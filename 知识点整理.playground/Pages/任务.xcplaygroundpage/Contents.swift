//: [Previous](@previous)

import Foundation

/*:
 1. swift通过命令行工具怎么运行，与C混编？
   > 直接执行即可
   > 与C混编，需要使用桥接文件，然后直接调用即可，需要考虑到swift与C桥接类型转换
 2. 协议拓展
 3. 泛型协议拓展使用
 4. 写一个Swift的脚本用来根据JSON生成属性信息 使用映射功能
 *
 playground变慢，先强退Xcode，然后执行命令：
 
 `killall -9 com.apple.CoreSimulator.CoreSimulatorService`
 
 然后重新打开即可
 */
//: [Next](@next)
//
